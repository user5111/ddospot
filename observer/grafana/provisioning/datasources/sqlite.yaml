apiVersion: 1
datasources:
  - name: DDoSPotWarehouse
    uid: DDoSPotWarehouse
    type: frser-sqlite-datasource
    access: proxy
    isDefault: true
    jsonData:
      path: /var/lib/grafana/warehouse.sqlite3
      queries:
        - "SELECT 1"
